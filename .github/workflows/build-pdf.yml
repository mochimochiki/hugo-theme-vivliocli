name: Build PDF

on:
  push:
    branches: [ add-docker ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build PDF
      run: |
        docker build . --file Dockerfile --tag hugo-theme-vivliocli
        docker run -v $(pwd):/hugo-theme-vivliocli --init --rm --cap-add=SYS_ADMIN hugo-theme-vivliocli pdf

    - uses: actions/upload-artifact@v2
      with:
        name: pdf
        path: exampleSite/public_pdf/jp/Guide.pdf